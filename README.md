# net-guardian
Python-based internet watchdog that detects connection changes, prevents notification spam, sends desktop alerts, measures speed, logs status history, and runs silently in the background.
